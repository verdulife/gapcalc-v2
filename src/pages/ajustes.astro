---
import { papers, plotters, tshirts, vars } from "@/lib/consts";
import Layout from "@/layouts/Layout.astro";
import OptionsComponent from "@/components/Options.svelte";
---

<Layout title="Ajustes">
  <OptionsComponent
    client:load
    papers={papers}
    plotters={plotters}
    tshirts={tshirts}
    vars={vars}
  />
</Layout>
