---
import Layout from "@/layouts/Layout.astro";
import PlotterComponent from "@/components/Plotter.svelte";
import { getAjustesData } from "@/lib/db-ajustes";

const data = await getAjustesData();
---

<Layout title="Plotter">
  <PlotterComponent client:load plotters={data.plotters} vars={data.vars} />
</Layout>