---
import { papers, prints, vars } from "@/lib/consts";
import Layout from "@/layouts/Layout.astro";
import Print from "@/components/Print.svelte";
---

<Layout title="Impresión">
  <Print client:load papers={papers} prints={prints} vars={vars} />
</Layout>
