---
import Layout from "@/layouts/Layout.astro";
import Print from "@/components/Print.svelte";
import { getAjustesData } from "@/lib/db-ajustes";

const data = await getAjustesData();
---

<Layout title="Impresión">
  <Print client:load papers={data.papers} prints={data.prints} vars={data.vars} />
</Layout>